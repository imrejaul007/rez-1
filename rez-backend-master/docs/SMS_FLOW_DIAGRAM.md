# SMS Notification System - Flow Diagrams

## 📱 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     SMS Notification System                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐        ┌──────────────┐       ┌───────────┐ │
│  │   Orders     │───────▶│  SMSService  │──────▶│  Twilio   │ │
│  │  Route       │        │              │       │    API    │ │
│  └──────────────┘        └──────────────┘       └───────────┘ │
│                                  │                      │       │
│  ┌──────────────┐                │                      │       │
│  │  Products    │────────────────┘                      │       │
│  │   Route      │                                       │       │
│  └──────────────┘                                       │       │
│                                                          ▼       │
│  ┌──────────────┐                               ┌──────────────┐│
│  │   Future     │                               │   Customer   ││
│  │   Routes     │                               │    Phone     ││
│  └──────────────┘                               └──────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Order Status Update Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Order Status Update Flow                             │
└─────────────────────────────────────────────────────────────────────────┘

    Merchant App/Dashboard
            │
            │ PUT /api/merchant/orders/:id/status
            │ { status: "preparing", notifyCustomer: true }
            ▼
    ┌────────────────────┐
    │  Orders Route      │
    │  (orders.ts)       │
    └────────────────────┘
            │
            ├─── Validate Status ✓
            │
            ├─── Update Order in DB ✓
            │
            ├─── Get Merchant Info
            │    (Business Name)
            │
            ├─── Check notifyCustomer flag
            │         │
            │         │ if true
            │         ▼
            │    ┌──────────────────┐
            │    │  SMSService      │
            │    │  .sendOrderStatus│
            │    │  Update()        │
            │    └──────────────────┘
            │              │
            │              ├─── Format Phone Number
            │              │    (+91XXXXXXXXXX)
            │              │
            │              ├─── Build Message
            │              │    ("Your order #XXX is preparing...")
            │              │
            │              ├─── Check Twilio Config
            │              │         │
            │              │         ├─── Configured ──▶ Send via Twilio
            │              │         │                        │
            │              │         │                        ▼
            │              │         │                  [Customer Phone]
            │              │         │                        │
            │              │         │                        ▼
            │              │         │                   ✅ SMS Sent
            │              │         │
            │              │         └─── Not Configured ──▶ Console Log
            │              │                                  │
            │              │                                  ▼
            │              │                             📝 Logged Only
            │              │
            │              └─── Error? ──▶ Log Warning
            │                                (Don't fail request)
            ▼
    Return Success Response
    { success: true, data: order }
```

---

## 📦 Low Stock Alert Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Low Stock Alert Flow                               │
└─────────────────────────────────────────────────────────────────────────┘

    Merchant App/Dashboard
            │
            │ PUT /api/merchant/products/:id
            │ { inventory: { stock: 2, lowStockThreshold: 5 } }
            ▼
    ┌────────────────────┐
    │  Products Route    │
    │  (products.ts)     │
    └────────────────────┘
            │
            ├─── Validate Product Data ✓
            │
            ├─── Update Product in DB ✓
            │
            ├─── Sync to User-Side Product
            │
            ├─── Check: stock <= lowStockThreshold?
            │         │
            │         │ if true
            │         ▼
            │    ┌──────────────────┐
            │    │  Get Merchant    │
            │    │  Phone Number    │
            │    └──────────────────┘
            │              │
            │              ▼
            │    ┌──────────────────┐
            │    │  SMSService      │
            │    │  .sendLowStock   │
            │    │  Alert()         │
            │    └──────────────────┘
            │              │
            │              ├─── Format Phone Number
            │              │    (+91XXXXXXXXXX)
            │              │
            │              ├─── Build Message
            │              │    ("⚠️ Low stock: Product has 2 units left...")
            │              │
            │              ├─── Check Twilio Config
            │              │         │
            │              │         ├─── Configured ──▶ Send via Twilio
            │              │         │                        │
            │              │         │                        ▼
            │              │         │                  [Merchant Phone]
            │              │         │                        │
            │              │         │                        ▼
            │              │         │                   ✅ SMS Sent
            │              │         │
            │              │         └─── Not Configured ──▶ Console Log
            │              │                                  │
            │              │                                  ▼
            │              │                             📝 Logged Only
            │              │
            │              └─── Error? ──▶ Log Warning
            │                                (Don't fail request)
            ▼
    Return Success Response
    { success: true, data: product }
```

---

## 🔐 Merchant OTP Flow (For Future Implementation)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Merchant OTP 2FA Flow                              │
└─────────────────────────────────────────────────────────────────────────┘

    Merchant Login Attempt
            │
            │ POST /api/merchant/auth/login
            │ { email, password }
            ▼
    ┌────────────────────┐
    │  Auth Route        │
    └────────────────────┘
            │
            ├─── Validate Credentials ✓
            │
            ├─── Generate OTP (6 digits)
            │
            ├─── Store OTP in DB/Cache
            │    (with expiry: 10 minutes)
            │
            ▼
    ┌──────────────────┐
    │  SMSService      │
    │  .sendMerchantOTP│
    └──────────────────┘
            │
            ├─── Format Phone Number
            │
            ├─── Build Message
            │    ("123456 is your OTP for ABC Store...")
            │
            ├─── Send via Twilio
            │
            ▼
    [Merchant Phone]
            │
            │ Merchant enters OTP
            ▼
    ┌────────────────────┐
    │  Verify OTP        │
    └────────────────────┘
            │
            ├─── OTP Valid? ──▶ Login Success ✓
            │
            └─── OTP Invalid? ──▶ Login Failed ✗
```

---

## 📨 SMSService Internal Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    SMSService.send() Method Flow                        │
└─────────────────────────────────────────────────────────────────────────┘

    SMSService.send({ to, message })
            │
            ▼
    ┌──────────────────────────┐
    │ Check Twilio Config      │
    │ (SID, Token, Phone)      │
    └──────────────────────────┘
            │
            ├─────────────────┬─────────────────┐
            │                 │                 │
        CONFIGURED      NOT CONFIGURED    ERROR
            │                 │                 │
            ▼                 ▼                 ▼
    ┌─────────────┐   ┌─────────────┐   ┌─────────────┐
    │ Send via    │   │ Console Log │   │ Throw Error │
    │ Twilio API  │   │ SMS Details │   │             │
    └─────────────┘   └─────────────┘   └─────────────┘
            │                 │
            │                 │
    ┌───────▼─────────────────▼───────┐
    │  twilioClient.messages.create() │
    │  {                              │
    │    body: message,               │
    │    from: TWILIO_PHONE_NUMBER,   │
    │    to: formatted_phone          │
    │  }                              │
    └─────────────────────────────────┘
            │
            ▼
    ┌──────────────────┐
    │ Log Success/Fail │
    │ with Message SID │
    └──────────────────┘
            │
            ▼
    Return (Success or Throw Error)
```

---

## 🔄 Phone Number Formatting Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│              Phone Number Formatting Flow (E.164)                       │
└─────────────────────────────────────────────────────────────────────────┘

Input: "9876543210" or "+919876543210"
    │
    ▼
┌──────────────────────────┐
│ formatPhoneNumber()      │
│ (countryCode = '+91')    │
└──────────────────────────┘
    │
    ├─── Remove non-numeric chars
    │    "9876543210" ──▶ "9876543210"
    │    "+91-9876543210" ──▶ "919876543210"
    │
    ├─── Check if starts with country code
    │         │
    │         ├─── Yes (919876543210)
    │         │     │
    │         │     └──▶ Add '+': "+919876543210" ✅
    │         │
    │         └─── No (9876543210)
    │               │
    │               └──▶ Add country code: "+919876543210" ✅
    │
    ▼
Output: "+919876543210" (E.164 format)
```

---

## 🌐 Error Handling Strategy

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Error Handling Philosophy                            │
└─────────────────────────────────────────────────────────────────────────┘

Every SMS Operation is Wrapped in Try-Catch:

try {
    │
    ├─── Format Phone Number
    │
    ├─── Send SMS
    │
    └─── Log Success

} catch (error) {
    │
    ├─── Log Warning (console.warn)
    │
    ├─── Don't throw/rethrow
    │
    └─── Continue main operation
}

WHY?
  ┌─────────────────────────────────────────────────────┐
  │ SMS is a NOTIFICATION, not a CRITICAL OPERATION     │
  │                                                      │
  │ Main operations (order update, product update)      │
  │ should NEVER fail just because SMS failed           │
  │                                                      │
  │ SMS failures are logged for monitoring              │
  │ but don't disrupt user experience                   │
  └─────────────────────────────────────────────────────┘
```

---

## 📊 SMS Message Templates

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      SMS Message Templates                              │
└─────────────────────────────────────────────────────────────────────────┘

ORDER CONFIRMATION:
┌──────────────────────────────────────────────────────────────────────┐
│ Your order #[ORDER_NUMBER] from [STORE_NAME] has been confirmed!    │
│ We'll notify you once it's ready for delivery.                      │
└──────────────────────────────────────────────────────────────────────┘

ORDER PREPARING:
┌──────────────────────────────────────────────────────────────────────┐
│ Your order #[ORDER_NUMBER] from [STORE_NAME] is being prepared.     │
│ We'll update you soon!                                              │
└──────────────────────────────────────────────────────────────────────┘

ORDER READY:
┌──────────────────────────────────────────────────────────────────────┐
│ Good news! Your order #[ORDER_NUMBER] from [STORE_NAME] is ready    │
│ for pickup/delivery!                                                │
└──────────────────────────────────────────────────────────────────────┘

LOW STOCK ALERT:
┌──────────────────────────────────────────────────────────────────────┐
│ ⚠️ Low stock alert: [PRODUCT_NAME] has only [STOCK] unit(s) left.   │
│ Consider restocking soon!                                           │
└──────────────────────────────────────────────────────────────────────┘

NEW ORDER ALERT:
┌──────────────────────────────────────────────────────────────────────┐
│ 🎉 New order #[ORDER_NUMBER] from [CUSTOMER_NAME]!                  │
│ Total: ₹[AMOUNT]. Login to your merchant dashboard to process.      │
└──────────────────────────────────────────────────────────────────────┘

MERCHANT OTP:
┌──────────────────────────────────────────────────────────────────────┐
│ [OTP_CODE] is your OTP for [MERCHANT_NAME] merchant login.          │
│ Valid for 10 minutes. Do not share this OTP with anyone.            │
└──────────────────────────────────────────────────────────────────────┘
```

---

## ⚡ Quick Integration Checklist

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     Integration Checklist                               │
└─────────────────────────────────────────────────────────────────────────┘

When Adding SMS to New Route:

1. Import SMSService
   ┌────────────────────────────────────────────┐
   │ import SMSService from '../services/SMSService'; │
   └────────────────────────────────────────────┘

2. Import Merchant Model (if needed)
   ┌────────────────────────────────────────────┐
   │ import { Merchant } from '../models/Merchant';   │
   └────────────────────────────────────────────┘

3. Wrap SMS call in try-catch
   ┌────────────────────────────────────────────┐
   │ try {                                      │
   │   await SMSService.send(...);              │
   │ } catch (error) {                          │
   │   console.warn('SMS failed:', error);      │
   │ }                                          │
   └────────────────────────────────────────────┘

4. Always format phone numbers
   ┌────────────────────────────────────────────┐
   │ const phone = SMSService.formatPhoneNumber(raw); │
   └────────────────────────────────────────────┘

5. Use specific methods when available
   ┌────────────────────────────────────────────┐
   │ await SMSService.sendOrderStatusUpdate(...); │
   │ // Better than generic send()              │
   └────────────────────────────────────────────┘
```

---

## 🎯 Implementation Status Visual

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Implementation Status                                │
└─────────────────────────────────────────────────────────────────────────┘

Core Service:         [██████████] 100% ✅
Orders Integration:   [██████████] 100% ✅
Products Integration: [██████████] 100% ✅
Environment Config:   [██████████] 100% ✅
Dependencies:         [██████████] 100% ✅
Documentation:        [██████████] 100% ✅
Testing Ready:        [██████████] 100% ✅

Overall Progress:     [██████████] 100% COMPLETE ✅
```

---

*SMS Flow Diagram Documentation*
*Visual Guide to SMS Notification System*
*Last Updated: November 17, 2024*
